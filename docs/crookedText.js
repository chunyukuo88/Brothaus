class crookedText extends HTMLElement {
    constructor(){
        super();
        const shadow = this.attachShadow({mode: 'open'});

        const sentence = document.createElement('p');
        

    }
}